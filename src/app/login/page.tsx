export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-surface px-6">
      <section className="w-full max-w-md rounded-md border border-line bg-white p-8">
        <p className="text-sm font-semibold text-brand">Marketing Agency ERP</p>
        <h1 className="mt-3 text-3xl font-semibold text-ink">관리자 로그인</h1>
        <p className="mt-4 text-base leading-7 text-slate-600">
          카카오 인증은 다음 단계에서 연결합니다. 지금은 라우트가 존재하는지 확인하기 위한 자리표시자 페이지입니다.
        </p>
      </section>
    </main>
  );
}
